---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: tools.webapp.my.domain
spec:
  group: webapp.my.domain
  names:
    kind: Tools
    listKind: ToolsList
    plural: tools
    singular: tools
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Tools is the Schema for the tools API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ToolsSpec defines the desired state of Tools
            properties:
              description:
                description: Description specifies the description of the tool
                type: string
              documentation:
                description: Documentation specifies the documentation of the tool
                type: string
              homepage:
                description: Homepage specifies the homepage of the tool
                type: string
              id:
                description: Id specifies the id of the tool
                type: string
              name:
                description: Name specifies the name of the tool
                type: string
              repository:
                description: Repository specifies the repository of the tool
                type: string
              versions:
                description: Versions specifies all versions of the tool
                items:
                  properties:
                    major:
                      type: integer
                    minor:
                      type: integer
                    patch:
                      type: integer
                  type: object
                type: array
            type: object
          status:
            description: ToolsStatus defines the observed state of Tools
            properties:
              error:
                description: Error indicates the error message of the tool
                type: string
              errorCount:
                description: ErrorCount indicates the number of times the tool has
                  failed
                type: integer
              errorTime:
                description: ErrorTime indicates the time when the error occurred
                format: date-time
                type: string
              installed:
                description: Installed indicates whether the tool is installed in
                  a users namespace
                type: boolean
              running:
                description: Running indicates whether the tool is running
                type: boolean
              version:
                description: Version indicates the version of the tool
                properties:
                  major:
                    type: integer
                  minor:
                    type: integer
                  patch:
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
